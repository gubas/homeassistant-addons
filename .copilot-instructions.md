# Instructions GitHub Copilot - Home Assistant Add-ons

## 📦 Structure du dépôt

Ce dépôt contient des add-ons Home Assistant :
- **3D Print Queue** (`3d_print_queue/`) - Version actuelle dans `config.yaml`
- **WLED Icons** (`wled_icons/`) - Version actuelle dans `config.json`

## 🚀 Commande de release

Pour créer une nouvelle release d'un add-on, utilise cette commande :

**Format :**
```
release <addon_name> <version> <description>
```

**Exemples :**
```
release 3d_print_queue 0.3.1 "Correction bug champ nom optionnel"
release wled_icons 0.7.6 "Amélioration interface"
```

## 📋 Processus de release automatique

Quand tu reçois une commande `release`, tu dois :

### Pour 3D Print Queue :
1. Mettre à jour `3d_print_queue/config.yaml` : ligne `version: "X.X.X"`
2. Ajouter dans `3d_print_queue/CHANGELOG.md` (après `# Changelog`) :
   ```markdown
   ## [X.X.X] - YYYY-MM-DD
   
   ### Fixed / Added / Changed
   - [Description]
   ```
3. Git commit : `🚀 3D Print Queue vX.X.X - [Description]`
4. Git tag : `3d_print_queue/vX.X.X`
5. Git push origin main + tags

### Pour WLED Icons :
1. Mettre à jour `wled_icons/config.json` : ligne `"version": "X.X.X"`
2. Ajouter dans `wled_icons/CHANGELOG.md` (après `# Changelog`) :
   ```markdown
   ## [X.X.X] - YYYY-MM-DD
   
   ### [Catégorie]
   - 🎨 [Description]
   ```
3. Git commit : `🚀 WLED Icons vX.X.X - [Description]`
4. Git tag : `wled_icons/vX.X.X`
5. Git push origin main + tags

## 🏷️ Convention de tags

- Format : `addon_name/vX.X.X`
- Exemples : `3d_print_queue/v0.3.1`, `wled_icons/v0.7.6`

## 🔢 Versioning sémantique

- **X.0.0** - Breaking changes
- **0.X.0** - Nouvelles fonctionnalités
- **0.0.X** - Bug fixes

## 📝 Catégories CHANGELOG

**3D Print Queue :** Added, Changed, Fixed, Removed  
**WLED Icons :** Utilise des emojis (🎨, 🐛, ✨, 📝, etc.)

## ⚠️ Important

- Toujours utiliser la date du jour au format ISO (YYYY-MM-DD)
- Ne pas oublier de push les tags : `git push origin --tags`
- Les deux add-ons ont des formats de config différents (YAML vs JSON)
